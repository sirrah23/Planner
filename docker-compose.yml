version: '2'
services:
  web:
    build: .
    environment:
      - DATABASE_URL=planner
      - DATABASE_TEST_URL=planner_test
    ports:
     - "5000:5000"
    command: python manager.py runserver --host 0.0.0.0
    volumes:
     - .:/app
    links:
      - db:mongo
    depends_on:
      - db
  db:
    image: mongo:3.4.6
    ports:
      - "27017:27017"
